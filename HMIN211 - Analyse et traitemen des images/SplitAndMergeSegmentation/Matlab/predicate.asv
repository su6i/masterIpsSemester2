function flag = predicate(region)
global sdThr
sd = std2(region);
m = mean2(region);
% flag = (sd > sdThr) & (m > 0) & (m < 100);
% flag = (sd > sqrt(10));
if
    flag = (sd > sdThr) & (m > 100);
else
    flag = (sd > sdThr) & (m > 100);
end